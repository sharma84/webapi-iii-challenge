// code away!
//yarn add express
//import server and require is to import express module
const server = require("./server.js");

server.listen(9090, () => {
  console.log("listening on port 9090");
});
